set -e

source ~/.bash_aliases

mpi_out=$1
if [[ ! -f "$mpi_out" ]]; then
    echo "Must specify mpi_out"
    exit
fi


while true; do
    clear
    mqjobs
    tail -n 1000 | grep Global $mpi_out | tail -n 200
    sleep 30
done
